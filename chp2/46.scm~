(define (make-vect v)
  (cons (car v) (cdr v)))
(define (xcor-vect v)
  (car v))
(define (xcor-vect v)
  (cdr v))

(define (add-vect v1 v2)
  (make-vect (+ (xcor-vect v1)
		(xcor-vect v2))
	     (+ (ycor-vect v1)
		(ycor-vect v2))
	     
